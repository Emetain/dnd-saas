spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://postgres:5432/dndsaas}
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME:dndsaas}
    password: ${DB_PASSWORD:dndsaas}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

