plugins {
    // Lets Gradle download the JDK 22 toolchain automatically when it is not installed.
    id("org.gradle.toolchains.foojay-resolver-convention") version "0.9.0"
}

rootProject.name = "dnd-saas-backend"
